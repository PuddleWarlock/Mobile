**Часть 1: Список датчиков**

1.  **Создан новый модуль** «Lesson5».
2.  **В виде списка отображено датчиковое оборудование** на устройстве.
3.  **В файл разметки `activity_main` добавлен `ListView`**.
4.  **В классе `MainActivity` реализовано получение списка датчиков** с помощью `SensorManager.getSensorList(Sensor.TYPE_ALL)`.
5.  **Список датчиков отображен в `ListView`** с использованием `HashMap` и `SimpleAdapter`.

![studio64_XH66fL9G2C](https://github.com/user-attachments/assets/46ec939b-d63f-4662-902c-2cb9cfcbcbbb)

**Часть 2: Показания акселерометра**

1.  **Создан новый модуль** с названием «Accelerometer».
2.  **Разработано приложение, отображающее значения акселерометра** на главном экране.
3.  **При вращении устройства значения изменяются** на главном экране.
4.  **Реализован экран с 3 текстовыми полями** в `activity_main.xml`.
5.  **Реализован интерфейс `SensorEventListener`** в классе `MainActivity`.
6.  **Создан объект класса `Sensor`** для акселерометра.
7.  **Датчик инициализирован** в методе `onResume`.
8.  **Регистрация датчика отменена** в методе `onPause`.
9.  **Отслеживание изменений значений акселерометра выполнено** в методе `onSensorChanged` и обновлены текстовые поля на экране.

![studio64_jHuAxg1YKE](https://github.com/user-attachments/assets/c60bc215-babd-4a44-9d0b-67f461035cd4)


**Часть 3: Механизм разрешений**

1.  **В рамках выполнения заданий 4 и 5, где требовалось использование системных функций, требующих разрешений, учтены следующие требования:**
    *   **Требуемые разрешения указаны в `AndroidManifest.xml`** в корневом теге `<manifest>` с элементом `<uses-permission>`.
    *   **Реализована проверка текущего статуса разрешения** с помощью `ContextCompat.checkSelfPermission()`.
    *   **В случае отсутствия разрешения реализован запрос разрешения** у пользователя с помощью вызова метода `requestPermissions()`.
    *   **В качестве аргументов метода `requestPermissions()` указаны** контекст активности, массив идентификаторов разрешений и код запроса.
    *   **Обработка результата взаимодействия с пользователем реализована** в методе `onRequestPermissionsResult()`, который переопределен в активности.
    *   **Проверка разрешения выполняется всегда** перед выполнением операции, требующей определенного разрешения.

**Часть 4: Камера**

1.  **Создан новый модуль** с названием «Camera».
2.  **Разработано приложение, которое вызывает системное приложение «камера»**, сохраняет изображение в папку приложения и отображает снимок на экране.
3.  **В файл разметки `activity_main` добавлен `image_view`**.
4.  **Реализован обработчик нажатия** с помощью `ViewBinding`.
5.  **Реализован вызов системного приложения камеры** с использованием намерений со значением действия `MediaStore.ACTION_IMAGE_CAPTURE`.
6.  **Использован метод `registerForActivityResult`** для формирования неявного намерения.
7.  **Использован `onActivityResult`** для получения результата.
8.  **В намерении размещен путь для сохранения результата** в виде `Uri` с ключом `MediaStore.EXTRA_OUTPUT`.
9.  **Использован `FileProvider`** для безопасной передачи файлов.
    *   **`FileProvider` определен в `AndroidManifest.xml`**.
    *   **Создан XML-файл** (`paths.xml` в `res/xml`), содержащий все пути, которые `FileProvider` будет использовать совместно с другими приложениями.
    *   **Действительный `URI` связан** в `Intent` и активирован.
10. **Создан файл** (`createImageFile()`) перед вызовом приложения камеры, в который будет записан полученный результат.
11. **Получен `URI`** (`FileProvider.getUriForFile()`) на основе значений из манифест-файла.
12. **Использован механизм разрешений**, рассмотренный в Части 3, для реализации приложения камеры.

![studio64_2RzgU6Q0xz](https://github.com/user-attachments/assets/cf5fe0dc-3460-49de-88a0-5a278ca506eb)

![studio64_PXBgzSldtc](https://github.com/user-attachments/assets/f5df2cd5-67f8-40c5-be4c-9b3141dab94e)

![studio64_jz2GWF4wq9](https://github.com/user-attachments/assets/0c09854e-07be-4e9c-a750-74c6572514c6)

**Часть 5: Микрофон. MediaRecorder**

1.  **Создан новый модуль** с названием «AudioRecord».
2.  **Разработано приложение для работы с диктофоном**, позволяющее записывать аудио и его воспроизводить.
3.  **В `AndroidManifest.xml` добавлены разрешения** `RECORD_AUDIO` и `WRITE_EXTERNAL_STORAGE`.
4.  **Проверено наличие разрешения пользователя** на использование микрофона и запись во внешнее хранилище перед использованием `MediaRecorder`. Обработка наличия разрешений выполнена в `MainActivity` аналогично Части 3.
5.  **Реализован учет состояния нажатия кнопок** для исключения ситуации одновременной записи и воспроизведения. При нажатии одной кнопки другая кнопка становится недоступной.
6.  **Использован класс `MediaRecorder`** для осуществления аудио записи с настройкой источника, формата, типа сжатия и файла вывода. Реализованы методы для инициализации, запуска, остановки и высвобождения ресурсов.
7.  **Использован класс `MediaPlayer`** для воспроизведения записанного материала с указанием файла, подготовкой, запуском, остановкой и высвобождением ресурсов.
8.  **Вызов методов управления записью и воспроизведением реализован** в обработчиках нажатий на кнопки.

![studio64_A4P5qsItP2](https://github.com/user-attachments/assets/349876b2-4b5a-4ebc-9032-29d1a246db7e)

![studio64_50Pi8BDwgF](https://github.com/user-attachments/assets/73993f11-9949-430f-a5d3-0c683a5b54d3)

**Часть 6: Контрольное задание**

1.  **В контрольном задании «MireaProject» к ранее созданным фрагментам добавлен экран аппаратной части (`HardwareFragment`)** со следующим функционалом:
    *   **В приложение добавлены механизмы запроса разрешений** (реализовано в `MicrophoneFragment` и `CameraFragment` с использованием `ActivityResultLauncher`).
    *   **Создан экран (`SensorFragment`), в котором используется результат с магнитометра** для решения задачи определения направления на север (отображение азимута).
    *   **Создан экран (`CameraFragment`), в котором используется результат приложения «камера»** для решения задачи создания коллажа (добавление сделанных фотографий в `LinearLayout`).
    *   **Создан экран (`MicrophoneFragment`), в котором используется функционал микрофона** для решения задачи записи голосовой заметки.
2.  **Реализована навигация между `HardwareFragment` и фрагментами, использующими датчики, камеру и микрофон**, с помощью `NavController`.
3.  **В `HardwareFragment` добавлены кнопки** для перехода к `SensorFragment`, `CameraFragment` и `MicrophoneFragment`.

![studio64_pLJB1QwM4d](https://github.com/user-attachments/assets/d619227b-4ca9-4f3e-b9b2-09503fb7816a)

![studio64_8aAhtdH3U5](https://github.com/user-attachments/assets/663d3e17-debb-4774-ab24-8a87b7c5c681)

![studio64_v7WCnIT5ZC](https://github.com/user-attachments/assets/67a6405c-50de-4e7e-9d83-1067979bc543)

![studio64_gcCwUBaOOd](https://github.com/user-attachments/assets/930afd38-e835-4808-928d-a485d0205368)

![studio64_uhTGKyunB4](https://github.com/user-attachments/assets/d6fa6acc-152f-4742-8036-efaa4d722121)

![studio64_VgNmmK11cV](https://github.com/user-attachments/assets/dc39bd79-a7c2-457b-9dcf-18db2c523a3e)
